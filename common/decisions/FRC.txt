FRC_war_cant_be_avoided = {
	frc_fontaine_republic_is_going_to_declare_war_on_us = {
		icon = GFX_decision_generic_prepare_civil_war 
		activation = {
			always = no 
		}
		available = {
			always = no 
		}
		days_mission_timeout = 30 
		timeout_effect = {
			add_war_support = 0.05 
			effect_tooltip = {
				FRA = {
					declare_war_on = {
						target = ROOT 
						type = annex_everything 
					}
				}
			}
		}
	}
	fra_immigrant_are_coming = {
		icon = GFX_decision_eng_blackshirt_march 
		activation = {
			always = no 
		}
		days_mission_timeout = 7 
		available = {
			has_war = yes 
		}
		timeout_effect = {
			activate_mission = fra_immigrant_are_coming 
			set_temp_variable_to_random = {
				var = the_population_to_add
				max = 3000 
				min = 2000 
			}
			add_manpower = the_population_to_add 
			multiply_temp_variable = {
				the_population_to_add = 0.33 
			}
			round_temp_variable = the_population_to_add 
			hidden_effect = {
				random_state = {
					limit = {
						is_core_of = FRA 
						not = {
							is_controlled_by = ROOT 
						}
					}
					add_manpower = the_population_to_add 
				}
				random_state = {
					limit = {
						is_core_of = FRA 
						not = {
							is_controlled_by = ROOT 
						}
					}
					add_manpower = the_population_to_add 
				}
				random_state = {
					limit = {
						is_core_of = FRA 
						not = {
							is_controlled_by = ROOT 
						}
					}
					add_manpower = the_population_to_add 
				}
			}
		}
	}
	frc_the_supply_issue = {
		icon = GFX_decision_generic_consumer_goods 
		days_mission_timeout = 90 
		activation = {
			always = no 
		}
		available = {
			OR = {
				num_of_civilian_factories > 24 
				AND = {
					controls_state = 16 
					controls_state = 18 
					controls_state = 17 
					controls_state = 27 
				}
			}
		}
		remove_effect = {
			add_stability = 0.05 
			add_political_power = 50 
		}
		timeout_effect = {
			add_stability = -0.01 
			if = {
				limit = {
					not = {
						has_idea = fra_short_of_consum 
						has_idea = fra_supply_issue 
						has_idea = fra_run_out_everything 
					}
				}
				add_ideas = fra_short_of_consum 
			}
			else_if = {
				limit = {
					has_idea = fra_short_of_consum 
				}
				swap_ideas = {
					remove_idea = fra_short_of_consum 
					add_idea = fra_supply_issue 
				}
			}
			else_if = {
				limit = {
					has_idea = fra_supply_issue 
				}
				swap_ideas = {
					remove_idea = fra_supply_issue 
					add_idea = fra_run_out_everything 
				}
			}
			if = {
				limit = {
					not = {
						has_idea = fra_run_out_everything 
					}
				}
				activate_mission = frc_the_supply_issue 
			}
		}
	}
	frc_demand_foodstuff = {
		icon = GFX_decision_generic_assassination 
		available = {
			any_state = {
				is_fully_controlled_by = ROOT 
			}
		}
		visible = {
			has_completed_focus = FRC_emergency_actions 
			check_variable = {
				demand_times < 3 
			}
		}
		days_remove = 30 
		cost = 50 
		complete_effect = {
			add_stability = -0.05 
			custom_effect_tooltip = can_decrease_the_debuff
			add_to_variable = {
				demand_times = 1 
			}
			if = {
				limit = {
					has_idea = fra_run_out_everything 
				}
				swap_ideas = {
					remove_idea = fra_run_out_everything 
					add_idea = fra_supply_issue 
				}
			}
			else_if = {
				limit = {
					has_idea = fra_supply_issue 
				}
				swap_ideas = {
					remove_idea = fra_supply_issue 
					add_idea = fra_short_of_consum 
				}
			}
			else = {
				remove_ideas = fra_short_of_consum 
			}
			add_days_mission_timeout = {
				mission = frc_the_supply_issue 
				days = 60
			}
			random_owned_controlled_state = {
				limit = {
					not = {
						has_state_flag = has_demanded 
					}
				}
				add_extra_state_shared_building_slots = 2 
				set_state_flag = has_demanded
			}
		}
	}
	frc_compulsory_labor = {
		icon = GFX_decision_generic_merge_plant_materiel 
		visible = {
			not = {
				has_country_flag = start_compulsory_labor_policy 
			}
			has_completed_focus = FRC_emergency_actions 
		}
		days_remove = -1 
		cancel_trigger = {
			not = {
				has_country_flag = start_compulsory_labor_policy 
			}
		}
		cost = 50 
		modifier = {
			political_power_gain = -0.15 
			industrial_capacity_factory = 0.1 
			industrial_capacity_dockyard = 0.1 
			line_change_production_efficiency_factor = 0.05 
			consumer_goods_expected_value = -0.05 
			production_factory_max_efficiency_factor = -0.05 
		}
		complete_effect = {
			set_country_flag = start_compulsory_labor_policy
		}
	}
	frc_pause_the_policy = {
		icon = GFX_decision_generic_merge_plant_materiel 
		visible = {
			has_country_flag = start_compulsory_labor_policy 
		}
		complete_effect = {
			clr_country_flag = start_compulsory_labor_policy 
		}
	}
	frc_demand_capitans = {
		icon = GFX_decision_generic_fundraising 
		fire_only_once = yes 
		cost = 100 
		visible = {
			has_completed_focus = FRC_emergency_actions 
		}
		days_remove = 15 
		modifier = {
			political_power_cost = 1
		}
		remove_effect = {
			add_stability = -0.05 
			add_offsite_building = {
				type = industrial_complex
				level = 4 
			}
			add_offsite_building = {
				type = arms_factory
				level = 2 
			}
		}
	}
	frc_ban_private_trade = {
		icon = GFX_decision_generic_arrest 
		visible = {
			has_completed_focus = FRC_emergency_actions 
			not = {
				has_country_flag = ban_private_trade 
			}
		}
		cost = 20 
		days_remove = -1 
		cancel_trigger = {
			not = {
				has_country_flag = ban_private_trade 
			}
		}
		modifier = {
			consumer_goods_expected_value = -0.03
			industrial_capacity_factory = 0.05 
			industrial_capacity_dockyard = 0.05 
			local_resources_factor = 0.1 
			political_power_factor = -0.15 
		}
		complete_effect = {
			add_stability = -0.01 
			set_country_flag = ban_private_trade
			if = {
				limit = {
					not = {
						has_country_flag = has_banned_once 
					}
				}
				add_days_mission_timeout = {
					mission = frc_the_supply_issue 
					days = 60 
				}
			}
			set_country_flag = has_banned_once
		}
	}
	frc_pause_the_ban = {
		icon = GFX_decision_FIN_stop_motti_tactics 
		visible = {
			has_country_flag = ban_private_trade 
		}
		complete_effect = {
			add_stability = 0.01 
			clr_country_flag = ban_private_trade 
		}
	}
}